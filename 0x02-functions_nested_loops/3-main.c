#include <stdio.h>
#include "main.h"
/**
 * main -coding
 *
 * Return: 0
 */
int _islower(int c);
int main(void)
{
	int c;

	if (c >= 'a' && c <= 'z')
	{
		_putchar(0);
	}
	else
	{
		_putchar(1);
	}
	return (0);
}

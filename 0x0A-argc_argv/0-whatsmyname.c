#include <stdio.h>

/**
 * main - coding
 * Return: 0
 * @argc : number of commend line
 * @argv : char of arry of command line
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
